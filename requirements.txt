# Personal AI Tech Radio — core dependencies
# Install with: pip install -r requirements.txt

# ── News collection ───────────────────────────────────────────────────────────
feedparser>=6.0.11          # RSS/Atom feed parsing (Module 5 — agent tools)

# ── Vector database ───────────────────────────────────────────────────────────
chromadb>=0.5.0             # Persistent vector store (Module 3)
sentence-transformers>=2.7  # Embeddings for semantic search (Module 3/4)

# ── HTTP client ───────────────────────────────────────────────────────────────
requests>=2.31.0            # Ollama API + RSS fallback (Module 1)

# ── Text-to-Speech ────────────────────────────────────────────────────────────
gTTS>=2.5.0                 # Google TTS — online, high quality (TTS engine)
pyttsx3>=2.90               # Offline TTS fallback (TTS engine)

# ── Utilities ─────────────────────────────────────────────────────────────────
pydantic>=2.0               # Data validation

# ── Fine-tuning (Module 2 — optional, skip if no GPU/disk space) ─────────────
# Uncomment to enable the LoRA fine-tuning demo:
#
# transformers>=4.40.0
# torch>=2.1.0
# peft>=0.10.0
# datasets>=2.19.0
# accelerate>=0.29.0
